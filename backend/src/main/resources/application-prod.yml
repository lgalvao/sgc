spring:
  datasource:
    url: jdbc:oracle:thin:@desenvolvimentobd:1521:admdes2
    username: sgc
    # SENTINEL: Hardcoded password removed. Must use environment variable in production.
    password: ${DB_PASSWORD}
  jpa:
    hibernate.ddl-auto: validate
    properties:
      hibernate.default_schema: sgc
    show-sql: false
    defer-datasource-initialization: false
  sql:
    init:
      mode: never
      platform: oracle

aplicacao:
  ambiente-testes: false
  jwt:
    secret: ${JWT_SECRET}

logging:
  level:
    sgc: INFO
    org.hibernate: WARN
    org.springframework: WARN
