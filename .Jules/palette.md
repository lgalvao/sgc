## 2025-02-19 - Login Autofocus
**Learning:** Even with pre-filled inputs in DEV, `autofocus` on the main action field reduces cognitive load and ensures consistent behavior across environments.
**Action:** Always check for primary input fields on entry pages and add `autofocus` if appropriate.
