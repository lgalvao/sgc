interface:
  display_name: "Refatoração de Testes"
  short_description: "Melhorar qualidade dos testes do SGC substituindo reflexão por comportamento, padronizando AssertJ e ajustando código de produção para testabilidade quando necessário"
  default_prompt: "Use $refatoracao-testes para melhorar a qualidade dos testes deste arquivo, substituindo testes de métodos privados por testes comportamentais, padronizando assertions com AssertJ e — quando necessário — aplicando padrões de testabilidade no código de produção."
